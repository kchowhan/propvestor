<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Violation Notice</title>
  <style>
    body {
      font-family: 'Times New Roman', serif;
      font-size: 11pt;
      line-height: 1.6;
      color: #000;
      margin: 0;
      padding: 20px;
    }
    .header {
      text-align: center;
      margin-bottom: 30px;
      border-bottom: 2px solid #000;
      padding-bottom: 20px;
    }
    .header h1 {
      margin: 0;
      font-size: 18pt;
      font-weight: bold;
      text-transform: uppercase;
    }
    .association-info {
      margin-top: 10px;
      font-size: 10pt;
    }
    .date-section {
      text-align: right;
      margin-bottom: 30px;
    }
    .recipient-section {
      margin-bottom: 30px;
    }
    .recipient-name {
      font-weight: bold;
      font-size: 12pt;
      margin-bottom: 5px;
    }
    .recipient-address {
      line-height: 1.8;
    }
    .subject-line {
      font-weight: bold;
      font-size: 12pt;
      margin: 30px 0 20px 0;
      text-transform: uppercase;
    }
    .violation-details {
      background-color: #f5f5f5;
      border: 1px solid #ccc;
      padding: 15px;
      margin: 20px 0;
    }
    .violation-details h3 {
      margin-top: 0;
      font-size: 12pt;
      border-bottom: 1px solid #333;
      padding-bottom: 5px;
    }
    .detail-row {
      margin: 10px 0;
    }
    .detail-label {
      font-weight: bold;
      display: inline-block;
      min-width: 120px;
    }
    .content-section {
      margin: 30px 0;
      white-space: pre-wrap;
    }
    .severity-badge {
      display: inline-block;
      padding: 4px 12px;
      border-radius: 4px;
      font-weight: bold;
      font-size: 10pt;
      margin-left: 10px;
    }
    .severity-MINOR {
      background-color: #fff3cd;
      color: #856404;
    }
    .severity-MODERATE {
      background-color: #ffeaa7;
      color: #d63031;
    }
    .severity-MAJOR {
      background-color: #fab1a0;
      color: #c0392b;
    }
    .severity-CRITICAL {
      background-color: #ff7675;
      color: #ffffff;
    }
    .letter-type {
      font-size: 10pt;
      color: #666;
      font-style: italic;
      margin-bottom: 10px;
    }
    .footer {
      margin-top: 50px;
      padding-top: 20px;
      border-top: 1px solid #ccc;
      font-size: 9pt;
      color: #666;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>{{associationName}}</h1>
    <div class="association-info">
      {{#if associationAddress}}{{associationAddress}}<br>{{/if}}
      {{associationCity}}, {{associationState}} {{associationPostalCode}}
    </div>
  </div>

  <div class="date-section">
    <strong>Date:</strong> {{date sentDate}}
  </div>

  <div class="recipient-section">
    <div class="recipient-name">{{homeownerName}}</div>
    <div class="recipient-address">
      {{homeownerAddress}}
    </div>
  </div>

  <div class="subject-line">
    Re: {{subject}}
  </div>

  <div class="letter-type">
    {{#eq letterType "FIRST_NOTICE"}}First Notice of Violation{{/eq}}
    {{#eq letterType "SECOND_NOTICE"}}Second Notice of Violation{{/eq}}
    {{#eq letterType "FINAL_NOTICE"}}Final Notice of Violation{{/eq}}
    {{#eq letterType "HEARING_NOTICE"}}Notice of Hearing{{/eq}}
    {{#eq letterType "CUSTOM"}}Custom Notice{{/eq}}
  </div>

  <div class="violation-details">
    <h3>Violation Details</h3>
    <div class="detail-row">
      <span class="detail-label">Violation Type:</span>
      <span>{{violationType}}</span>
    </div>
    <div class="detail-row">
      <span class="detail-label">Severity:</span>
      <span>{{violationSeverity}}<span class="severity-badge severity-{{violationSeverity}}">{{violationSeverity}}</span></span>
    </div>
    <div class="detail-row">
      <span class="detail-label">Violation Date:</span>
      <span>{{date violationDate}}</span>
    </div>
    {{#if propertyName}}
    <div class="detail-row">
      <span class="detail-label">Property:</span>
      <span>{{propertyName}}{{#if unitName}} - Unit {{unitName}}{{/if}}</span>
    </div>
    {{/if}}
    {{#if propertyAddress}}
    <div class="detail-row">
      <span class="detail-label">Address:</span>
      <span>{{propertyAddress}}</span>
    </div>
    {{/if}}
    <div class="detail-row" style="margin-top: 15px;">
      <span class="detail-label">Description:</span>
    </div>
    <div style="margin-left: 120px; margin-top: 5px;">
      {{violationDescription}}
    </div>
  </div>

  <div class="content-section">
{{{content}}}
  </div>

  <div class="footer">
    <p>This notice is sent by {{associationName}} in accordance with the association's governing documents.</p>
    <p>If you have any questions or concerns regarding this violation, please contact the association management.</p>
  </div>
</body>
</html>

